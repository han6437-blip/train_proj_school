# 校园信息化服务工单分流与处理助手实施 Spec

> Status: Draft v0.1  
> Owner: TBD  
> Last updated: 2026-07-29  
> 项目边界：完成训练、评估、量化与本地/内网推理服务验证；不接入生产校园系统

---

## 前言

本项目面向学校信息化服务大厅中的高频技术支持场景：学生、教师和行政人员使用自然语言描述问题，系统需要从多轮对话中收集工单要素、判断应咨询哪个系统、是否需要查询校内信息、应分派到哪个服务队列，并在用户确认后创建工单。

项目并不试图让模型代替信息化中心解决全部校园事务。模型的职责是**理解、提取、澄清和路由**；校内实时事实、权限判断和任何有副作用的操作仍由确定性系统完成。最终产物是一个可在校内网络的 CPU 机器上运行、输出严格 JSON/工具调用的小模型服务，以及一套可复现实验与评估流程。

本文件参照“需求定义 → 技术选型 → 数据与评估 → 训练 → 量化部署 → 迭代”的完整路径编写。实施时应先做评估框架和 baseline，再制作训练数据与训练，避免“先训练、后发现无法证明提升”的问题。

---

## 1. 项目背景

### 1.1 业务场景与痛点

校园信息化中心每天会收到大量技术服务请求，例如：

- “宿舍网络今天一直认证失败，明天要线上考试。”
- “选课系统进不去，提示没有权限。”
- “校园卡刷不开图书馆闸机。”
- “明天早上第一节课，A 教 203 的投影仪没有信号。”
- “教师电脑需要安装统计软件。”

这类请求的共同特征是：表达方式不统一、首轮信息经常不完整、处理流程往往需要查询账号状态或地点归属、不能凭模型记忆判断真实处理人。人工坐席需要反复追问“哪个校区、哪栋楼、使用什么账号、何时发生、是否影响考试/授课”，再路由到网络、校园卡、教务系统或教室设备支持队列。

若直接将所有对话交给远端通用 LLM，会遇到成本、延迟、校内数据安全和输出不稳定问题；若全部采用关键词规则，又难以覆盖多轮改口、相对时间和自然语言的多样表达。因此选择一个小模型进行领域后训练：将高频、低复杂度、格式要求明确的理解任务迁移到校内模型服务，复杂事实与执行继续交给工具。

### 1.2 项目定位

项目名称：**校园信息化服务工单分流与处理助手**。

其定位是“校园服务大厅的前置理解层”，不是万能校园问答机器人，也不是自动审批系统。模型只处理下列信息化相关类别：

| 类别编码 | 服务范围 | 示例 |
|---|---|---|
| `campus_network` | 校园网、宿舍网、Wi-Fi、VPN、网络认证 | 连不上网、认证失败、VPN 不可用 |
| `identity_access` | 统一身份认证、密码/MFA、系统权限技术问题 | 无法登录、账号锁定、权限异常 |
| `academic_system` | 选课、教务、成绩查询等系统的技术访问异常 | 页面报错、无法进入、提交失败 |
| `campus_card` | 校园卡、门禁、闸机、消费终端的技术异常 | 刷卡失败、卡片失效、设备读不到卡 |
| `classroom_equipment` | 教室投影、讲台电脑、扩声、打印设备 | 投影无信号、讲台电脑无法开机 |
| `software_hardware` | 校属终端、教学科研软件安装/故障 | 软件打不开、安装申请、电脑外设问题 |
| `other_it` | 仍属于信息化服务但无法归入上述类别 | 其他校内数字服务问题 |

宿舍水电维修、学籍变更、成绩修改、奖助审批、医疗和心理咨询不属于本项目范围。系统遇到这类输入只做无关/转介，不生成事实性结论，也不创建信息化工单。

### 1.3 核心目标

**任务效果目标：**

- 从多轮校园服务对话中稳定抽取问题类别、摘要、用户角色、校区/楼宇/房间、受影响系统或设备、期望处理时间、紧急程度和确认意图。
- 根据信息是否齐全，正确选择“追问、查询、路由、确认、创建”中的下一步。
- 对“明早第一节课”“下周一下午”“考试前”等可计算表达在给定时间基准下进行规范化；无法确定的表达必须追问，不得猜测。
- 输出严格 JSON 或严格工具调用参数。

**可靠性与边界目标：**

- 模型绝不编造学生/教师身份、校园卡状态、账号权限、教室设备状态、处理人员、工单号或知识库结论。
- 所有外部事实必须来自输入中的工具结果；所有写操作都要求明确用户确认和编排层二次校验。
- 将真实校园数据脱敏后再用于训练，优先使用合成数据。

**效率目标：**

- 训练完成后，将最优模型量化成 GGUF，在校内服务器或信息化中心终端的 CPU 上运行。
- 在固定的目标硬件、上下文长度和运行参数下，短上下文常规请求的流式响应目标为 < 1.5 秒；实际验收以实测报告为准。

### 1.4 最终谁使用模型

使用者是学生、教师和行政人员；他们只通过校园服务大厅网页、小程序、公众号或内部聊天入口提交问题，不需要在每个人的电脑上部署模型。

模型由学校信息化中心统一部署为一份校内服务：

```text
学生 / 教师 / 行政人员
        ↓ 校园服务大厅（网页、小程序或聊天入口）
编排服务（鉴权、会话状态、策略与审计）
        ↓
校园工单模型服务（llama-server）
        ↓
模拟或生产适配器：统一认证 / 资产或设备台账 / 知识库 / 工单系统
```

本项目阶段只实现模型和模拟工具适配器；真实系统接入、权限、审计、灰度和运维属于后续工程工作。开发者个人本机部署仅用于空跑、评估、演示与调试。

---

## 2. 任务定义与输入输出

### 2.1 职责边界

划分原则只有一句话：**从对话和当前时间能推出来的归模型；必须查询校内真实系统、涉及权限或会产生实际影响的归工具和业务代码。**

| 模型负责 | 工具/系统负责 |
|---|---|
| 抽取类别、摘要、地点、系统/设备、期望时间、用户表达的紧急程度 | 查询统一身份认证状态、校园卡状态、资产/设备台账 |
| 相对时间规范化、跨轮状态合并、识别取消和确认 | 查询教室归属、服务队列、值班人员、工单状态和 SLA |
| 判断信息缺口、是否无关、下一步是否调用工具 | 检索经批准的知识库内容 |
| 选择工具、生成正确参数、读取工具返回值 | 实际建单、权限变更、密码重置、门禁授权、成绩/学籍操作 |
| 根据工具结果生成待确认或已完成的结构化状态 | 鉴权、幂等、审计、风控、通知和回滚 |

例如，模型可理解“宿舍 Wi-Fi 一直认证不上”，但不能断言“你的账号被锁定”；模型可识别“明早第一节课前”属于紧急时段，但不能声称“张老师会在八点到场”。这些都必须由工具返回。

### 2.2 统一输入格式

每一轮使用同一输入合同，训练、评估和部署共用：

```text
system: <角色、边界、字段合同、动作与工具规则>
history:
  <user 消息、assistant 自然语言消息、assistant.tool_calls、tool 消息>
current_state: <上一轮由编排层保存的结构化状态，可为空>
current_time: 2026-07-29 09:30
user_context: <脱敏 user_id、角色 student/teacher/staff、校区等有限信息>
available_tools: <当轮可调用工具及参数合同>
user_input: <最新用户输入>
```

`history` 必须保存真实轨迹：普通 assistant 回复保持自然语言；函数调用是 `assistant.tool_calls`；工具结果由 `tool_call_id` 关联。上一轮的结构化输出由系统写入 `current_state`，不能把完整 final JSON 塞到普通 assistant 文本中，否则训练会混淆“面向用户的话术”和“内部状态”。

### 2.3 动作状态机

本模型每轮只输出一个顶层动作：

| action | 条件 | 输出 |
|---|---|---|
| `clarify` | 路由/建单所需关键信息缺失或时间表达不够确定 | `missing_info` 与明确追问 |
| `tool_call` | 需要查询校内实时事实，或满足创建模拟工单的条件 | 工具名与严格 JSON 参数 |
| `final` | 无需工具，或工具结果已经足够形成待确认/完成状态 | 完整状态 JSON 与简短回复 |
| `unrelated` | 与校园信息化服务无关，且不应污染当前工单 | 无关标记与转介提示 |

标准流转路径：

1. **首次描述**：提取类别、摘要和已知槽位。`ticket_category`、`summary`、`location` 任一缺失时优先 `clarify`。
2. **信息补齐**：当三个路由必填字段已齐全，必要时先查用户账号/设备，再调用 `route_ticket`；优先级和期望时间为可选参数。
3. **路由返回**：工具返回可用队列/处理人时，模型生成“待创建确认”的 `final`；此时 `confirmation=false` 或 `null`，还未写入工单。
4. **用户确认**：用户针对上轮明确建单问题回答“好的/创建/可以”等，才调用 `create_ticket`。
5. **创建成功**：只有 `create_ticket` 返回成功的工单 ID 后，模型才可回复“已创建”。
6. **取消或改口**：如用户说“先不要建”“换到明天下午”“不是 A 教，是 B 教”，模型应清除受影响的路由结果，回到澄清或重新路由，不能沿用失效结论。

### 2.4 任务类型与示例

#### 2.4.1 追问澄清样本

输入：

```text
history: []
current_time: 2026-07-29 09:30
user_input: 投影仪没信号，马上要上课了
```

期望输出：

```json
{
  "action": "clarify",
  "ticket_category": "classroom_equipment",
  "summary": "投影仪无信号",
  "location": null,
  "priority": "high",
  "requested_time": null,
  "info_complete": false,
  "missing_info": ["location"],
  "reply_type": "ask_clarification",
  "reply": "请问是哪个校区、楼宇和教室？我好为您安排教室设备支持。"
}
```

“马上要上课”可以支持 `high`，但无法凭空得出具体教室或课程时间。

#### 2.4.2 查询与路由样本

输入：

```text
history:
  用户：A 教 203 的投影仪没信号，明天早上第一节课要用。
current_time: 2026-07-29 09:30
available_tools:
  - lookup_room_equipment(campus, building, room)
  - route_ticket(category, location, priority, requested_time)
```

“明天早上第一节课”如果学校课表时间未作为输入提供，不能硬编码为某个时刻；模型应保留日期 `2026-07-30` 或追问明确时间，具体策略在字段规则中统一。若先要核验教室设备，输出：

```json
{
  "action": "tool_call",
  "tool_name": "lookup_room_equipment",
  "arguments": {"campus": null, "building": "A教", "room": "203"}
}
```

#### 2.4.3 最终结果样本

输入：

```text
history:
  用户：A 教 203 的投影仪没信号，下午两点前需要处理。
  assistant.tool_calls：lookup_room_equipment({"campus":null,"building":"A教","room":"203"})
  工具：{"room_id":"A-203","device":"projector","owner_queue":"classroom-support"}
  assistant.tool_calls：route_ticket({"category":"classroom_equipment","location":"A教203","priority":"high","requested_time":"2026-07-29 14:00"})
  工具：{"queue":"classroom-support","assignee":{"name":"陈工","id":"tech_07"},"status":"available"}
  assistant：已找到教室设备支持人员陈工，下午两点前可处理，需要我创建工单吗？
  用户：好的，创建吧
current_time: 2026-07-29 09:30
```

正确下一步是调用 `create_ticket`，不是直接宣称已经创建：

```json
{
  "action": "tool_call",
  "tool_name": "create_ticket",
  "arguments": {
    "category": "classroom_equipment",
    "summary": "投影仪无信号",
    "location": "A教203",
    "priority": "high",
    "requested_time": "2026-07-29 14:00",
    "assignee_id": "tech_07"
  }
}
```

### 2.5 唯一字段合同

所有 Prompt、数据集、工具参数、服务端状态和评分器使用以下唯一合同。未知单值一律使用 JSON `null`；禁止用“暂无”“未知”“无”等字符串替代 `null`。

| 字段 | 类型与唯一表示 |
|---|---|
| `ticket_category` | 上述七类枚举之一或 `null` |
| `summary` | 不超过约 40 字的中文问题概述；只包含输入已知事实 |
| `user_role` | `student`、`teacher`、`staff` 或 `null`；来自 `user_context` 或用户明确表达 |
| `affected_system` | 系统/应用名称字符串或 `null` |
| `affected_asset` | `{ "asset_id": string\|null, "device_name": string\|null }`；ID 只能来自工具 |
| `location` | 规范化为“校区 + 楼宇 + 房间/宿舍”可用部分的字符串，未知为 `null` |
| `priority` | `low`、`normal`、`high`、`critical` 或 `null` |
| `requested_time` | `YYYY-MM-DD`、`YYYY-MM-DD HH:MM` 或 `null` |
| `assignee_name` / `assignee_id` | 仅当工具返回时存在，否则为 `null` |
| `routing_status` | `available`、`unavailable`、`pending` 或 `null` |
| `confirmation` | 针对“是否创建工单”的明确回答，取 `true`、`false`、`null` |
| `info_complete` | 路由最小必填字段是否齐全 |
| `missing_info` | 仅允许 `ticket_category`、`summary`、`location`，按此顺序输出 |
| `unrelated` | 最新消息是否不属于校园信息化支持 |

优先级按保守规则归一化：

- `critical`：多名用户或关键校园服务发生明显中断，并有输入证据，例如“全班都无法进行在线考试”。
- `high`：授课、考试、重要教学活动或当前学习/办公被明确阻塞，例如“下午两点授课投影无信号”。
- `normal`：一般的单用户故障、软件安装和常规技术支持。
- `low`：不影响当前使用的咨询、优化需求或可延后处理的请求。

模型不能仅因用户使用“急”“快点”就把优先级定为 `critical`，更不能擅自推断考试或教学影响。

### 2.6 时间规范化规则

相对时间以输入 `current_time` 为唯一锚点：

- “今天下午两点” → 当天 `14:00`。
- “明天” → 次日日期；未给时间时为 `YYYY-MM-DD`。
- “下周一” → 算出下一个自然周一的日期。
- “明早十点” → 次日 `10:00`。
- “第一节课前”“考试前”“尽快”等没有被输入明确定义的时段，**不可臆测学校作息表**；保留可确定日期或将 `requested_time` 设为 `null`，并追问。

评估集必须固定 `current_time`、明确预期值；训练数据不得依赖模型自行读取真实日期。

### 2.7 工具合同

首版仅保留与模型决策相关的少量工具，由后端收敛复杂分支。所有工具都必须出现在当轮 `available_tools` 中，模型不得发明工具名。

| 工具 | 用途 | 调用前提 |
|---|---|---|
| `lookup_account_status` | 查询统一身份认证或目标系统账号的受限状态 | 用户已描述账号/访问问题，且需核实状态 |
| `lookup_room_equipment` | 查询教室及设备台账归属 | 已知楼宇/教室，且为教室设备问题 |
| `lookup_campus_card` | 查询校园卡有效性或读卡状态 | 用户明确为校园卡/闸机技术异常 |
| `search_knowledge_base` | 查询经批准的自助处理指引 | 问题类别和摘要已清楚，允许先检索 |
| `route_ticket` | 查询支持队列、处理人和可处理时段 | 类别、摘要、地点齐全 |
| `create_ticket` | 创建模拟或真实工单 | 用户确认且路由成功，编排层另行校验 |

示例工具调用：

```json
{
  "action": "tool_call",
  "tool_name": "route_ticket",
  "arguments": {
    "category": "campus_network",
    "summary": "宿舍 Wi-Fi 认证失败",
    "location": "东校区6栋412",
    "priority": "high",
    "requested_time": "2026-07-30"
  }
}
```

`create_ticket` 是有副作用的操作。模型输出工具调用不等于系统必须执行；编排层必须检查会话用户身份、二次确认、字段完整性、请求幂等键和权限策略。

---

## 3. 技术选型

### 3.1 基座模型选择

本项目的模型不是为了开放问答或长篇写作，而是为了中文多轮抽取、结构化输出和工具调用。因此候选模型优先比较以下能力：

- 中文校园语境（楼栋、教室、选课、统一认证、校园卡）的理解和短确认识别。
- 多轮状态继承与用户改口后的状态覆盖。
- 严格 JSON、枚举与 `null` 输出稳定性。
- 相对时间标准化和工具调用参数遵循。
- 小尺寸模型在 GGUF CPU 量化后的速度和内存。
- 许可证、LLaMA-Factory 支持与 llama.cpp 转换兼容性。

首轮候选建议为 Qwen3 1.7B/4B 与 InternLM 同档模型。先在冻结评估集跑未微调 baseline，再决定主训练基座；不能仅凭通用榜单或单个 Demo 选型。

### 3.2 后训练方法选择

#### 3.2.1 SFT：把任务合同训练稳定

监督微调是主线。SFT 训练“多轮上下文 + 当前消息 → 正确 action/JSON/工具调用”的映射，重点学习：

- 校园服务类别区分与摘要压缩。
- 必填信息缺口判断和最小追问。
- 工具调用的时机、名称和参数。
- 对工具结果的读取；只有结果中出现的事实才能写入 final。
- 创建工单前的确认边界。
- 枚举、时间、`null`、数组与无额外 Markdown 的稳定输出。

使用 LoRA/QLoRA 可降低训练成本；`train_on_prompt=false`、`mask_history=true`，只对当前目标回复计算损失，避免模型复述系统提示或旧轮内容。

#### 3.2.2 DPO：定向压制高风险错误

SFT 形成可用基线后再做 DPO。DPO 样本并非泛泛的“回答 A 比 B 好”，而是围绕高风险边界构造：

- 信息不完整时跳过澄清、强行路由。
- 未查询账号/校园卡/教室设备就编造状态。
- 将“好的”错误当作创建确认，或把“先不要”仍解释为确认。
- 用户更改地点或时间后继续使用旧的处理人。
- 工具失败后假装成功，或输出不存在的工单号。

`chosen` 为严格正确答案；`rejected` 可通过程序化定向扰动生成，例如虚构 `assignee_id`、补出未出现的宿舍号、把 `clarify` 改成 `create_ticket`。DPO 目标是降低这些错误，不是让模型变得过度保守或拒绝正常请求。

### 3.3 模型压缩方法选择

部署目标是校内 CPU 服务，故采用“训练后合并 + GGUF 量化”的路径：

1. 将最优 LoRA 合并到基座，导出 FP16 权重。
2. 转换为 GGUF。
3. 使用代表性校园支持语料计算重要性矩阵（imatrix，可选但建议比较）。
4. 默认生成 Q4_K_M；必要时对比 Q5_K_M、Q8_0。
5. 在同一冻结集和同一运行参数下比较 FP16/各量化档位质量与速度。

量化不是单纯压小文件。对本项目而言，若 Q4 出现 JSON 损坏、工具参数漏失或确认判断明显退化，应先测量再上调位宽；不能因模型大小符合预期就忽略功能回退。

### 3.4 训练与推理框架选择

**训练：**LLaMA-Factory 主线，覆盖 SFT、DPO、LoRA/QLoRA、数据集注册与模型模板。若必须实现定制损失/采样逻辑且框架配置无法覆盖，才下沉到 TRL。

**推理：**选择 `llama.cpp` 的 `llama-server`。它能原生加载 GGUF，在 Windows/Linux CPU 上运行，支持 OpenAI 兼容接口，且上下文长度、线程、batch、slot 等参数可直接记录，适合量化实验。Ollama 可用于快速展示，但不作为正式性能对比的唯一运行时；vLLM、TGI、TensorRT-LLM 等 GPU 高并发框架不属于本项目的 CPU 部署主线。

**环境分工：**本地 CPU 用于工程脚手架、数据校验、空跑训练、评估和部署验证；正式训练可短时租用云端 4090 等 GPU。训练结束后下载产物并关闭实例，控制成本。

---

## 4. 数据集制作

### 4.1 两层训练样本

训练集必须同时含“中间过程”和“最终结果”两层：

- **中间过程样本：**应该追问时输出 `clarify`；该查账号、校园卡、教室设备或路由时输出 `tool_call`。评分重点是动作、工具名和参数。
- **最终结果样本：**工具结果已返回或无需工具时输出 `final`。评分重点是状态字段、确认、事实可追溯和回复边界。

只训练最终 JSON 会导致模型在真实会话中急于给出结果，无法可靠地完成查询与确认流程。

### 4.2 SFT 数据格式

采用 ShareGPT 结构，使用模型原生 chat template。示意：

```json
{
  "conversations": [
    {"from": "system", "value": "你是校园信息化服务工单助手。只输出符合合同的 JSON。"},
    {"from": "user", "value": "history: []\ncurrent_time: 2026-07-29 09:30\nuser_input: 宿舍网认证一直失败"},
    {"from": "assistant", "value": "{\"action\":\"clarify\",\"ticket_category\":\"campus_network\",\"missing_info\":[\"location\"],\"reply\":\"请问是哪个校区、宿舍楼和房间？\"}"}
  ]
}
```

真实工具调用样本使用目标模型支持的 function/tool-call 格式，而不是只在自然语言中拼写“调用某函数”。`dataset_info.json` 中明确注册 SFT 和 preference 数据集，并记录数据版本。

### 4.3 DPO 偏好对格式

每对共享同一 prompt：

```json
{
  "prompt": "<多轮历史、current_state、current_time 与当前用户消息>",
  "chosen": "{\"action\":\"clarify\",\"missing_info\":[\"location\"],...}",
  "rejected": "{\"action\":\"tool_call\",\"tool_name\":\"route_ticket\",\"arguments\":{\"location\":\"东校区6栋412\"}}"
}
```

上述 rejected 的问题是凭空补出地点并跳过追问。每个 rejection 尽量只包含一个可解释的边界错误，避免模型仅从明显语法错误中学习。

### 4.4 数据规模、类别配比与难例

首版建议 SFT 约 1,500 条，DPO 约 300–500 对，验证集独立划分。初始配比可根据真实服务量调整：

| 类别 | 建议 SFT 占比 | 重点场景 |
|---|---:|---|
| 校园网络 | 20% | 宿舍网、Wi-Fi、VPN、认证失败 |
| 身份与访问 | 16% | 统一认证、MFA、账号/权限技术异常 |
| 教务系统 | 16% | 选课、成绩、教务页面访问或提交故障 |
| 校园卡 | 12% | 刷卡、门禁、闸机、消费终端技术异常 |
| 教室设备 | 18% | 投影、讲台电脑、扩声、教学紧急情况 |
| 软件硬件 | 10% | 校属软件、电脑、打印与外设 |
| 无关/风险边界 | 8% | 非 IT 事项、取消、越权、工具失败 |

每类都要涵盖首次描述、补充地点、工具返回、待确认、确认、取消/改口；不能只有单轮分类样本。下列难例应单独上采样：

- 相对时间及无法确定的作息表达。
- “好”“不用了”“先等等”“换到明天”这类短确认/改口。
- 长历史下的状态冲突，例如先说宿舍后改为教室。
- 输入中故意出现同学、老师或处理人姓名，模型不得据此编造分派结果。
- 工具 `unavailable`、超时、找不到设备或无匹配账号。
- 学籍、成绩修改、心理咨询等不在范围的请求。

### 4.5 数据来源与合规

首版优先采取“场景模板 + 强模型生成 + 规则校验 + 人工抽检”的方式。工具结果全部使用合成实体，例如 `stu_1024`、`CARD-***`、`A-203` 和 `tech_07`，不要使用真实学号、姓名、宿舍、教室排课、账号或工单号。

若未来使用历史工单，必须完成：数据使用授权、去标识化、访问隔离、最小字段保留、敏感文本过滤、留存期限与删除机制。不得将原始聊天记录、身份证号、联系方式、健康信息或成绩信息直接用于模型训练。

### 4.6 质量校验与隔离纪律

入库前自动检查：

- JSON 能解析，且没有 Markdown 代码块、额外前后缀。
- Schema、枚举、字段类型、`null` 和 `missing_info` 排序符合合同。
- 时间相对 `current_time` 计算正确；不确定表达没有伪造具体时刻。
- 工具调用只使用可用工具和所需参数；`create_ticket` 必须先有路由和确认语义。
- 输出中的账号、校园卡、资产、教室设备、处理人和工单事实均可从输入/工具结果追溯。
- 同一会话在用户取消、变更地点或变更时间后不复用失效状态。
- 按对话骨架、场景模板、实体和近重复文本进行去重，防止 train/val/test 泄漏。

`test.jsonl` 在 M0 baseline 前冻结，之后不参与训练，也不因某个模型失败而修改。新增失败案例先放入候选池，版本迭代时另建测试集或 holdout。

---

## 5. 评估方案

### 5.1 评估集结构

测试样本存为 `test.jsonl`，每条建议含 `id`、`input`、`expected`、`assertions`、`tags` 和 `allowed_facts`：

```json
{
  "id": "classroom_relative_time_001",
  "input": {
    "history": [],
    "current_time": "2026-07-29 09:30",
    "user_input": "明早十点前帮我看一下 A教203 的投影"
  },
  "expected": {"action": "tool_call", "ticket_category": "classroom_equipment", "requested_time": "2026-07-30 10:00"},
  "assertions": ["tool_name=lookup_room_equipment", "requested_time=2026-07-30 10:00"],
  "tags": ["relative_time", "classroom_equipment", "tool_call"],
  "allowed_facts": ["A教203", "投影"]
}
```

评估集至少覆盖主路径、工具调用、相对时间、多义确认、长上下文、无关请求、取消改口、工具失败与幻觉诱饵，并按服务类别均衡抽样。另保留独立 holdout，只在阶段性最终选择时使用，防止长期围绕冻结集调参而刷分。

### 5.2 指标定义

| 指标 | 口径 |
|---|---|
| JSON 合法率 | 原始输出可解析；不对格式损坏做自动修复后计成功 |
| Schema 合规率 | 动作形状、字段、类型、枚举、`null`、数组均符合合同 |
| 动作准确率 | `clarify`/`tool_call`/`final`/`unrelated` 严格匹配 |
| 工具调用联合准确率 | action、工具名、必填参数和值全部正确才成功 |
| 类别准确率 | `ticket_category` 严格枚举匹配 |
| 槽位 F1 | 地点、系统、设备、优先级、时间等分别统计再汇总 |
| 时间准确率 | 以固定 current_time 严格对比规范结果 |
| 确认准确率 | 专门统计短回复、取消和改口，避免被常规样本稀释 |
| 幻觉率 | 无输入/工具证据的校园事实、处理人、工单号或知识结论均记错 |
| 性能 | 首 token、总耗时、tokens/s、内存；固定硬件与运行参数 |

摘要 `summary` 可进行 NFKC、标点和大小写归一化，再使用关键词覆盖或本地中文 embedding 辅助评估；但类别、时间、工具参数、资产/人员事实均必须严格匹配，不使用外部 LLM Judge。

### 5.3 Baseline 与分数卡

M0 需要同时评估远端强模型（只作能力上限参照）和多个未微调候选基座。每次输出包括：

- 模型、权重、量化档位、Prompt/数据集版本、生成参数和硬件信息。
- 总体指标与按类别、action、tag 的细分指标。
- JSON/Schema 失败、事实幻觉、工具调用错误、确认误判的失败样本清单。
- P50/P95 延迟、tokens/s、峰值内存。

分数卡不仅用于排名，还用于决定训练数据的定向补强。例如若 `confirmation_ambiguous` 低于其他标签，应增加确认对话与 DPO 负例；若 Q4 仅在长上下文 JSON 合法率下滑，应检查上下文与量化参数，而不是盲目扩大训练集。

---

## 6. 实施规划

### 6.1 阶段一：工程脚手架与推理/评估框架

**目标：**先搭建可运行、可打分的闭环。此阶段不训练、不制作正式大规模数据。

工作项：

- 建立独立工程目录：`configs/`、`data/`、`prompts/`、`src/`、`scripts/`、`reports/`、`artifacts/`。
- 固化系统 Prompt、JSON Schema、工具合同和状态对象。
- 建立模型后端抽象，使远端 API 和 `llama-server` 共享推理调用方式。
- 实现 JSON 解析器、Schema/字段评分器、幻觉证据检查与性能计时。
- 用合成样本打通 `clarify`、`tool_call`、`final`、`unrelated` 四条路径。
- 搭起 mock 工具服务，返回确定性合成账号、设备、教室与路由结果。

**DoD：**一条命令可运行“一个模型后端 + 一份测试样本”，输出机器可读和 Markdown 分数卡；故意传入非法输出也能被准确标记。

### 6.2 阶段二：评估集制作与 Baseline（M0）

**目标：**先冻结衡量标准，再了解未微调模型的真实短板。

工作项：

- 编制 `test.jsonl` 与 holdout，确保类别、难例和工具路径覆盖。
- 为每条样本定义 expected、assertions、tags 和事实证据。
- 校验评估数据的 JSON/Schema/时间/去重质量并冻结版本 hash。
- 运行远端强模型与候选基座 baseline。
- 形成“弱项 → 后续数据配比”的基线分析报告。

**DoD（M0）：**冻结评估集、可复现 baseline 报告、候选基座和首轮训练目标明确。

### 6.3 阶段三：训练数据制作

**目标：**以 M0 暴露的弱项为依据制作 SFT/DPO 数据，而不是无差别堆量。

工作项：

- 构造约 1,500 条 SFT、多轮状态和工具轨迹样本。
- 构造约 300–500 对 DPO 定向偏好样本。
- 实现数据 lint 与泄漏检查；拆分 train/val，测试集保持隔离。
- 注册 LLaMA-Factory 数据集，写数据卡和版本日志。

**DoD：**所有训练文件通过质量校验、版本登记完成、与测试/holdout 零重叠。

### 6.4 阶段四：训练与实验矩阵（M1/M2）

**目标：**获得可回归的 SFT 和 DPO 权重，比较模型系列和尺寸。

建议矩阵：

| 实验 | 基座 | 方法 | 目的 |
|---|---|---|---|
| E0 | 0.5B 小模型 | SFT/DPO 各 2 steps | 本地空跑验证路径、模板和数据 |
| E1 | Qwen 候选 A | LoRA/QLoRA SFT | 建立领域微调基线 |
| E2 | E1 产物 | DPO | 压制幻觉、确认误判和越权创建 |
| E3 | InternLM 对照 | 同口径 SFT | 检验基座差异 |
| E4 | 最优模型 | 生成/上下文参数扫描 | 确定部署口径 |

正式训练在云端 GPU 上进行，但每个配置先完成 CPU 空跑。每次训练结束立刻用冻结集出分数卡；训练 loss 只作诊断，不能代替业务评估。DPO 从保守 beta、学习率和 epoch 开始，防止模型为躲避幻觉而大量输出澄清或拒绝。

**DoD：**M1 有最优 SFT 权重和相对 baseline 的分析；M2 有 DPO 权重及相对 SFT 的收益/代价分析。

### 6.5 阶段五：量化与校内服务验证（M3）

**目标：**将最优模型转为 CPU 可用的 GGUF 服务，验证性能提升没有造成质量崩塌。

- 合并 LoRA、导出 FP16、转 GGUF，按需计算 imatrix。
- 生成 Q4_K_M，必要时生成 Q5_K_M/Q8_0。
- 相同 Prompt、样本、生成参数、上下文和硬件下，对各版本评估质量与延迟。
- 用 `llama-server` 启动 OpenAI 兼容接口，并由阶段一 harness 实测端到端调用。
- 记录模型 hash、命令、线程、批大小、slot、上下文、CPU/RAM、操作系统与测试轮数。

**DoD（M3）：**一份可调用的本地/内网模拟服务和量化对比报告；默认量化档位有数据依据。

### 6.6 阶段六：迭代优化与数据闭环（M4）

**目标：**形成“评估暴露弱项 → 定向补样 → 再训练 → 回归”的持续改进路径。

- 从分数卡按 tags 聚类失败样本，例如时间、确认、工具错误、无关转介。
- 将失败模式抽象为合成或脱敏候选样本，人工审核后加入下一版训练数据。
- 对比增加样本、调整类别配比、改 Prompt、改工具协议和确定性后处理的效果。
- 记录每轮数据版本、参数、结果和是否达到收益递减。

**DoD（M4）：**至少完成一轮可复现的定向迭代，并用同一冻结集证明变化。

### 6.7 阶段七：项目报告

项目报告应完整记录：业务边界、数据治理、技术取舍、baseline、训练矩阵、量化前后质量与性能、风险、失败案例、成本与后续接入建议。报告需用数据说明“模型为校园服务流程带来了什么改善、付出了什么资源代价、哪些情况仍必须交给人工或工具”。

---

## 7. 部署、集成与回滚策略

### 7.1 本项目部署形态

本项目的验收部署采用单机或内网测试机形态：

- `llama-server` 承载 GGUF 模型，暴露 OpenAI 兼容 API。
- 编排服务负责组装 Prompt、保存 `current_state`、控制工具调用、执行 Schema 校验。
- Mock 工具服务模拟统一认证、校园卡、教室设备、路由和建单系统。
- 评估 harness 作为独立客户端，测量质量和性能。

这已经能够验证“本地模型 + 工具调用 + 工单状态机”的闭环，不需要在第一阶段把真实校园平台接入。

### 7.2 生产接入建议（预留）

后续若接入校园服务大厅，模型服务由信息化中心统一运行，用户无感访问。生产编排层必须补充：身份鉴权、会话隔离、权限策略、PII 脱敏日志、幂等建单、速率限制、审计、异常降级与人工接管。

对于多人并发，先以校内 CPU 服务和队列满足低并发入口；若服务量提升，再增加多实例、更多 CPU 资源或采用 GPU 服务。模型升级采用版本化模型文件和配置，先 shadow mode 离线比较，再灰度到小比例真实流量；出现质量/安全问题时将路由配置切回原规则或远端模型，不删除历史模型和评估记录。

### 7.3 关键安全原则

- 模型不能绕过服务端权限系统。
- 模型不会直接修改成绩、学籍、选课、门禁、账号权限或校园卡余额。
- `create_ticket` 仅创建服务请求，且要求明确确认、权限校验和幂等键。
- 对账号、成绩、身份等敏感对话，日志只保存最小化、脱敏后的评估字段。

---

## 8. 风险与开放问题

| 风险/问题 | 影响 | 建议处理 |
|---|---|---|
| 真实工单含个人信息 | 合规与隐私风险 | 首版完全使用合成数据；后续严格脱敏和审批 |
| 类别范围过大 | 数据和工具合同失控 | 首版只做四类高频信息化问题 |
| “第一节课”等时间不确定 | 时间标签不一致 | 不硬编码校历/课表；引入课表工具或统一追问 |
| 模型过度创建工单 | 产生无效请求 | 强制确认、DPO 负例、编排层策略校验 |
| 量化造成 JSON 或工具回退 | 服务不可用 | FP16/Q4/Q5/Q8 同口径对比，按红线选档 |
| 评估集被反复调参过拟合 | 虚高分数 | 冻结集 + 独立 holdout + 版本化数据 |
| CPU 多人并发不足 | 高峰体验下降 | 先测服务大厅峰值，后续扩容/多实例/升级硬件 |
| 模型许可证或校内部署限制 | 无法上线 | 训练前核对基座、embedding 和工具组件许可 |

仍需在项目启动时明确：目标校区/服务大厅入口、首版要覆盖的实际问题量、目标硬件、各类必填字段、优先级/SLA 定义、验收阈值，以及是否支持中英混合输入。

---

## 9. 推荐首个可运行切片

第一轮仅实现“教室设备故障”或“宿舍网络认证失败”之一，建议优先选择**教室设备故障**：它天然包含地点、紧急时间、设备台账、路由和用户确认，能够覆盖完整状态机。

最小流程：

```text
用户报告投影故障
  → 模型抽取类别/摘要，缺地点则追问
  → 已知教室后查询设备台账
  → 查询教室设备支持队列
  → 模型请求用户确认
  → 确认后创建模拟工单
  → 工具返回工单号后模型回复已创建
```

该切片通过 M0–M3 后，再按相同字段合同扩展到校园网络、统一身份认证、校园卡和教务系统。这样项目既能在早期展示完整链路，又不会因一开始堆叠过多业务分支而失去可控性。
