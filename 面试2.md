# 面试二：Agent 架构与生产化设计

> 项目口径：业务场景统一为企业机房和办公电脑故障咨询、维修工程师匹配及上门预约。在线流程采用 LangGraph `StateGraph`，由 Router、Supervisor 决策节点和专业 Agent 节点/子图组成。Agent 之间通过共享 State 与图的边协作，不把 Agent 注册成 Tool；Tool Calling 只用于维修知识检索、工程师查询、预约服务等业务能力。消息队列、Redis、RocketMQ、生产级 Checkpointer 都属于目标生产方案，当前材料不能证明已实际部署。

## 2、编排层、State 层、Tool 层、Observability 层各自怎么画

我会画成四层，并把安全边界单独标出来：

```text
Client / SSE
    │
API Gateway：认证、租户、限流、request_id、trace_id
    │
编排层：Router → Supervisor Decision → 专业节点/子图 → finalize/interrupt
    │                    │
State 层：Checkpoint + 任务阶段/槽位/证据引用/预算/待确认动作
    │                    │
Tool 层：RAG、时间解析、工程师查询、预约 Service、状态查询
    │
事实层：用户/权限/排班/预约/幂等/Outbox 数据库

横切 Observability：Trace → Graph/Node → Model/Retrieval/Tool/DB Span
```

编排层决定“下一步去哪个节点”，不直接决定预约是否允许创建。State 层分成可信 Runtime Context 和模型可写的任务 State：`tenant_id/user_id/role` 来自认证，模型不能修改；任务阶段、槽位和证据引用按 Schema 更新。Tool 层的模型输出只是建议，业务 Service 必须重新鉴权、检查资源归属、确认、幂等和冲突。Observability 使用统一 `trace_id/request_id/thread_id/operation_id/tool_call_id` 串联，但日志默认不保存完整 Prompt 和敏感参数。

## 3、LangGraph 的 checkpoint 怎么存，thread_id 和租户怎么映射，服务重启后任务怎么续跑

开发环境可以用内存或 SQLite Checkpointer，生产方案使用持久化 Checkpointer，例如 PostgreSQL Saver。LangGraph 按 `thread_id` 保存每一步状态快照；调用图时通过 `configurable.thread_id` 定位同一条执行线程。

不能信任客户端直接传来的 `thread_id`。我会建一张线程归属表，至少包含 `tenant_id、user_id、conversation_id、thread_id、status、state_schema_version`，由服务端根据认证身份查询或创建。即使 `thread_id` 不可猜，每次恢复仍要校验线程归属；Checkpoint 的存储命名空间也包含租户，防止 IDOR 和跨租户恢复。

服务重启后，客户端带业务会话 ID 重新请求，服务端完成认证并映射到可信 `thread_id`，读取最新 Checkpoint，从 `interrupt` 或未完成节点继续。恢复前必须重查预约、排班、确认有效期等数据库事实，不能把旧 State 当成最新事实。

最危险的是“副作用成功，但 Checkpoint 尚未写入”这个窗口，所以创建预约使用稳定 `operation_id` 和幂等记录。恢复时先按 `operation_id` 查询结果：已成功就把结果写回 State，结果未知就对账，明确失败才允许复用同一 Key 重试。Checkpoint 解决流程恢复，不代替业务事务。

## 4、多 Agent 编排：supervisor-worker 和 pipeline 模式在售后场景怎么选

Pipeline 适合步骤稳定、顺序清晰的流程，例如机房电脑维修预约通常是“提取资产编号、设备型号、故障现象和机房位置 → 澄清 → 查询对应技能工程师 → 展示候选时段 → 用户确认 → 创建预约”。这类流程应该尽量确定性化，便于测试状态机和副作用边界。

Supervisor-worker 适合任务路径依赖新 Observation 的场景，例如用户说“机房一台电脑频繁蓝屏，先判断是驱动还是硬盘问题，如果无法远程解决就安排工程师上门”；Supervisor 根据知识检索和排查结果决定是否进入预约节点。它的代价是额外模型调用、状态传递和循环风险，不能让所有请求都经过它。

本项目采用混合架构：明确单意图由 Router 走专业节点快路径；预约节点内部以 Pipeline/状态机为主；只有复合任务进入 Supervisor 决策循环。用户行为分析不作为在线 worker，而是消费业务事件异步运行，避免增加主链延迟和故障耦合。

## 5、意图路由层怎么设计，4 个业务 Agent（查商品/查订单/退换货/投诉）“帮我看看”怎么分发

结合机房维修场景，我会把面试官给出的四个业务域映射为：查商品对应“查电脑/服务器及备件信息”，查订单对应“查维修工单或备件订单”，退换货对应“更换或退回故障备件”，投诉对应“维修服务投诉”。四类业务设计成受控节点/子图，而不是注册成 Supervisor 的四个 Tool。入口路由分三层：

1. 按当前页面、按钮来源、业务状态和实体 ID 做确定性路由。
2. 用结构化意图模型输出 `intent、candidate_intents、entities、confidence、needs_clarification`。
3. 只有复合或低置信度请求进入 Supervisor，由条件边选择节点。

“帮我看看”本身不携带足够业务动作，先从 State 收集候选：当前查看的服务器或硬盘备件、刚才展示的维修工单、未完成的故障硬盘退换单、当前维修投诉。如果只有一个候选且只是只读查看，可以告诉用户“我先帮你查看刚才那台服务器的维修工单”后执行；有多个候选就做最小澄清：“你是想看设备或备件信息、维修工单、备件退换进度，还是维修服务投诉？”

即使路由选中了查工单节点，服务端仍要检查该工单和机房资产是否属于当前租户；备件退换和投诉等写操作必须补齐资产编号、备件序列号、原因和确认，路由置信度不能替代业务授权。

## 6、Tool Schema 怎么写能让 LLM 少选错，参数合法但业务非法（查了别人订单）怎么拦

Tool Schema 要窄、具体、可区分：工具名使用业务动词，如 `get_my_booking`、`list_available_engineers`，描述中明确适用条件和禁止条件；枚举代替自由文本，日期使用标准格式，必填和可空字段明确，拒绝未知字段；读工具和写工具分开，避免一个 `manage_order(action=...)` 包含过多动作。工具返回统一的成功、可重试错误、业务拒绝和需要澄清协议。

不过 Schema 只能解决“格式是否合法”，解决不了“业务是否允许”。查询订单时 Tool 不接受模型传入的 `tenant_id` 或 `user_id`，而是从可信 ActorContext 注入；仓储查询使用 `tenant_id + order_id`，再检查订单所有者、角色、授权范围和字段级权限。返回“不存在或无权访问”时避免泄漏资源是否存在。

写操作还要验证业务状态机、金额或次数限制、一次性确认记录、参数摘要、资源版本和幂等键。模型即使生成了完全合法的 JSON，也只有在 Service 校验通过后才执行。

## 7、Agentic RAG 和被动 RAG 区别，检索结果和工具返回冲突时以谁为准

被动 RAG 是固定链路：每个请求都做一次检索，再把结果交给模型生成。Agentic RAG 允许工作流判断是否需要检索、选择知识域、改写查询、追加检索、调用结构化业务工具，或者在证据不足时澄清/拒答。它更灵活，但步骤、成本和幻觉面也更大，所以要限制最大检索轮次和查询改写次数。

冲突不能简单规定“Tool 永远比 RAG 高”。要按事实类型、权威性和时效性判断：

- 订单、预约、排班、库存、权限等实时业务事实，以经过鉴权的业务数据库/Tool 为准。
- 电脑、服务器、交换机等设备的维修手册、故障说明和服务政策，以当前生效版本的知识库文档为准。
- 同一事实发生冲突时不让模型自行调和，要保留来源、版本和时间戳，触发重查或明确告知用户存在冲突。

例如维修服务文档说“机房应急故障支持周末上门”，但排班 Tool 返回该机房所在区域本周末没有具备服务器维修技能的工程师，最终必须以实时排班为准；回答可引用文档解释服务规则，同时说明本次实际可选时段。

## 8、多 Agent 幻觉放大怎么治，critic 节点和 citation 约束怎么接进图

多 Agent 幻觉放大的根因通常是把前一个 Agent 的自然语言结论当成了事实。我的做法是节点之间传结构化结果和证据引用，不传不可验证的长篇自由文本；重要字段带 `source_id、source_type、version、timestamp、confidence`，后续节点按自己的输入 Schema 投影。

在图中，咨询节点输出 `claims + citations + unresolved`，随后进入确定性证据校验节点：检查引用是否存在、是否属于当前租户与版本、每个关键 Claim 是否有支持片段。只有开放式答案或高风险结论才进入 Critic 节点，Critic 返回 `pass/revise/abstain` 和问题标签；修订最多一次，仍失败就拒答或转人工，避免 Critic 与生成节点无限互评。

Critic 只能提高内容质量，不能替代权限、确认和业务规则。创建预约仍由 Service 读取结构化参数并校验，不能因为 Critic 说“可信”就执行。

## 9、大促脉冲流量下，Agent 集群任务死锁/算力抢占怎么解，限流熔断降级放哪一层

当前项目没有可核验的大促实测，下面是生产设计。各层职责如下：

- **网关层**：按租户/用户限流、请求体限制、全局并发上限，超载返回 429/503 和 `Retry-After`。
- **编排层**：限制图步骤、Deadline、并行分支和每请求工具数；取消后停止未开始的只读任务。
- **模型网关**：按本地模型槽位做有界排队、优先级和准入；高风险任务保留资源，低风险抽取才允许切换到已验证备用模型。
- **Tool 层**：不同下游使用独立线程池/连接池/信号量做 Bulkhead；设置超时、有限重试、熔断和半开探测。
- **异步层**：队列按任务类型隔离，消费者有并发上限、租约、心跳、重试和死信队列；用队列最老年龄而不只是长度触发告警。

避免死锁要固定资源获取顺序、缩短数据库事务、禁止持锁等待远端模型/HTTP、使用租约和 fencing token 防止过期 Worker 回写。降级不能破坏安全不变量：精排失败可退到 RRF；开放咨询可以延后或转人工；预约写入不能跳过确认、鉴权、幂等和冲突约束。

## 10、Python 侧 SSE 断线重连、用户点停止后怎么真取消推理而不是假装停

SSE 事件应有递增序号，并把 `(operation_id, seq)` 编入 `id:`。服务端保留一个有期限的事件缓冲；标准 EventSource 重连携带 `Last-Event-ID`，服务端先校验该游标属于当前用户，再续传。缓冲过期时按 `operation_id` 查询数据库终态，而不是重新执行整个 Agent。业务成功只在事务提交后发送结构化 `final_result`，不能提前由模型输出“预约成功”。

停止推理要做协作式取消：前端发送取消命令或关闭连接，服务端设置 cancellation token，取消 LangGraph 运行和未完成的异步 Tool Task，并使用推理服务的请求 ID 调用取消接口；本地模型网关也要能从等待队列移除请求并中断生成。线程池中的同步函数通常不能被强制安全终止，因此需要短超时、可中断 SDK 或进程隔离，不能只取消外层 `await`。

取消边界必须明确：尚未产生副作用的模型和只读工具可以停止；已接受的预约命令不能因 SSE 断线自动撤销。若数据库提交结果未知，状态记为 `UNKNOWN` 并对账；取消已创建预约是另一条经过确认和幂等保护的业务命令。

## 11、RocketMQ 在 Agent 系统里跑什么（异步工具回调/审计/评测埋点），消息重复消费怎么幂等

当前项目只设计了消息队列链路，没有选定或部署 RocketMQ。若生产采用 RocketMQ，适合承载不阻塞在线回答的事件：预约后通知、长耗时工具回调、用户行为分析、审计投递、评测采样和记忆候选生成。在线事务采用 Transactional Outbox：预约、幂等记录和 Outbox 同事务写入，Publisher 再投递消息，避免数据库成功但消息永久丢失。

消息系统通常是至少一次语义，不能假设 Broker 永不重复。每条事件有稳定 `event_id`、`event_type`、聚合对象 ID、版本和 Trace Context；消费者先以 `event_id` 写 Inbox 唯一记录，并让 Inbox 与本地业务更新处于同一事务。重复消息命中唯一约束后直接返回已处理结果。调用短信等外部系统还要把稳定幂等键传给供应商；超时先查状态，不能换新 Key 盲重试。

消费失败按错误类型有限重试，参数或权限等永久错误直接进入死信/人工处理；监控重试次数、死信、消费延迟和 Outbox 积压。

## 12、RocketMQ 延迟消息做 Agent 定时兜底任务，和 Redis 过期回调比有什么坑

延迟消息适合提醒、超时扫描触发器，但不能把“到了投递时间”理解成“精确在该时刻执行”。Broker 负载、消费积压和故障恢复都会造成晚到；产品通常还有最大延迟范围、精度或消息量限制。消息可能重复，因此定时任务仍要带 `task_id/operation_id`，执行前检查任务当前状态和版本，做到幂等。长周期任务还要考虑修改、取消和重排，不能只发一条无法管理的延迟消息。

Redis Keyspace Notification 更不适合作为可靠调度器：过期事件在 Key 真正被删除时才产生，时间不精确；它基于 Pub/Sub，消费者离线时事件可能丢失；集群中通知是节点本地的。它可以做缓存清理提示，不能作为必须执行的预约兜底任务。

更稳妥的方式是把 `due_at、status、version、lease` 持久化到任务表，由调度器扫描到期任务并投递普通消息；延迟消息只是唤醒优化，定期扫描负责补漏。最终执行永远先查数据库状态，已完成或已取消就不再动作。

---

## 可核验参考

- [LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)
- [LangGraph Subgraphs](https://docs.langchain.com/oss/python/langgraph/use-subgraphs)
- [RocketMQ：消费重试](https://rocketmq.apache.org/docs/featureBehavior/10consumerretrypolicy/)
- [RocketMQ：延迟消息](https://rocketmq.apache.org/docs/featureBehavior/02delaymessage/)
- [RocketMQ：消费幂等最佳实践](https://rocketmq.apache.org/docs/bestPractice/01bestpractice/)
- [Redis：Keyspace Notifications](https://redis.io/docs/latest/develop/pubsub/keyspace-notifications/)
