# 面试三：Harness、评测、知识更新与 MCP

> 项目口径：业务案例以企业机房、服务器和办公电脑故障咨询及上门维修为主。当前项目以 LangGraph 编排、内部 Tool Registry/Function Calling 和业务 Service 为主。版本管理、灰度、实时知识同步和 MCP 属于生产化完善方案；没有代码、发布记录或监控数据支撑的部分，不描述成已经上线。

## 5、Harness 工程化：Agent 的 Prompt、Tool、State Schema 怎么做版本管理和灰度

我会把一次可运行的 Agent 配置视为不可变 Release Manifest，而不是只给 Prompt 单独打版本。Manifest 至少记录：

```text
release_id
graph_version / route_policy_version
prompt_version
tool_schema_version / tool_implementation_version
state_schema_version / checkpoint_migration_version
model + inference_config
retrieval_pipeline_version / index_snapshot
memory_policy_version
eval_suite_version
```

Prompt、Tool Schema、State Schema 都进入 Git 审查，发布后按内容哈希生成不可变版本；Trace 记录本次请求实际命中的全部版本，才能复现问题。Tool Schema 的兼容规则要明确：增加可选字段一般可向后兼容，删除/改名/改变语义需要新主版本；写工具不能让新旧语义共用同一个名字。State 带 `schema_version`，节点只接收自己的输入投影；旧 Checkpoint 恢复时先运行确定性迁移，迁移失败就转人工，不能把旧 State 直接交给新图。

灰度采用服务端稳定哈希，例如按 `tenant_id + user_id` 固定分桶，保证同一会话不在版本间漂移。发布顺序是：离线回归 → 历史 Trace Shadow Replay → 内部租户 → 小比例 Canary → 分阶段放量。灰度期间比较任务成功率、工具选错率、越权/副作用不变量、P95、Token 成本和人工接管率；高风险写操作可先只灰度理解和建议部分，执行层保持旧版本。回滚切换 Manifest 指针，但已经由新版本创建的 Checkpoint 必须由兼容层处理，不能简单回滚代码后强行读取。

## 6、多 Agent 系统评测集怎么建，任务成功率、工具选错率、人工接管率怎么量

评测集不能只收集单轮“标准问法”。我会从脱敏的真实会话中按故障类型、设备类型、操作系统、渠道、会话长度和失败类型分层抽样，再补充合成边界样本。至少覆盖：电脑无法启动、蓝屏、硬盘 SMART 告警、机房网络异常等明确单意图，故障咨询加维修预约的复合任务，多轮资产与时段修正、代词消歧、无答案、工具超时、排班冲突、恢复重放、越权查询其他机房资产、Prompt Injection 和跨租户负样本。

每个 Case 不只标最终答案，还标：期望路由/允许路由集合、关键 State 变化、必需和禁止的 Tool、参数约束、期望证据、是否应澄清/拒答/转人工，以及最终业务状态。对于开放式故障咨询允许多条有效轨迹；对于预约创建则用确定性不变量判定。

指标分层计算：

- **任务成功率** = 满足该 Case 全部成功条件的任务数 / 可评任务数。成功条件包括答案、关键槽位、正确业务终态和安全约束，不能只看最后一句文本。
- **工具选错率** = 错误工具决策数 / 可评工具决策数，并拆成误调用、漏调用、工具类型错误、参数错误、重复调用和调用顺序错误。
- **人工接管率** = 转人工会话数 / 符合统计范围的会话数；再区分“安全策略要求的合理接管”和“系统失败导致的被迫接管”。降低前者不一定是好事。

还要看路由准确率、槽位继承正确率、引用支持度、Recall@K、平均步骤数、恢复成功率、重复副作用数、跨租户泄漏数、P95 和单位成功任务成本。跨租户泄漏、未确认写入、重复预约等安全不变量必须为零，不能与平均质量分数相抵消。

规则程序负责 Schema、轨迹、数据库终态和引用存在性；人工专家负责业务正确性；LLM-as-Judge 只做按固定 Rubric 的辅助评分，并用人工样本校准。报告按难度和失败切片展示样本量与置信区间，避免一个总平均数掩盖关键问题。

## 7、RAG 知识库每天变（价格/库存/活动），增量更新和向量一致性怎么保证不召回旧文档

首先要划清边界：备件价格与库存、资产状态、工单和可预约时段是高频实时业务事实，不应该靠向量库回答，应该通过经过鉴权的业务 Tool 实时查询；RAG 更适合电脑/服务器维修手册、驱动与固件说明、故障知识和服务政策。回答中如果同时包含硬盘更换规范和当前备件库存，前者引用知识文档，后者以实时 Tool 返回为准。

对需要入库且经常更新的驱动公告、固件兼容说明和服务政策，我会采用版本化增量链路：

1. 业务库通过 CDC 或 Outbox 产生带 `doc_id、source_version、updated_at、event_id` 的变更事件。
2. 摄取 Worker 按内容哈希幂等处理，解析、切分并生成带版本的 Chunk；向量记录包含 `doc_id/chunk_id/source_version/valid_from/valid_to/status/tenant/ACL/embedding_version`。
3. 先把新版本写入候选索引并完成数量、哈希和抽样召回校验，再原子切换索引别名或查询所使用的 `active_snapshot`。
4. 切换后将旧版本标记为失效并异步清理。删除事件写 Tombstone，防止旧的乱序更新把文档复活。

查询时必须先过滤 `status=active`、当前业务版本、生效时间、租户和 ACL；语义缓存键包含索引快照、文档版本和权限版本，切换后旧缓存自然失效。不能先从全库召回再在最后过滤，因为旧文档可能已经占满 Top-K。

一致性采用“事件驱动增量 + 定期全量对账”：比较源表版本、文档清单、Chunk 哈希和向量数量，发现漏事件、重复或孤儿向量后修复。监控从源数据提交到索引可见的延迟、版本不一致数、旧版本命中数和失败队列；若新索引尚未通过校验，继续使用旧的完整快照，而不是暴露半更新索引。

## 8、MCP 和 Function Calling 在你系统里边界在哪，为什么不直接全上 MCP

两者不在同一层。Function Calling 是模型输出结构化工具名和参数的调用机制；MCP 是 AI Host 与外部 Server 之间发现和交换 Tools、Resources、Prompts 的标准协议，包含客户端/服务端、能力协商和会话边界。接入 MCP 后，模型最终仍可能以工具调用形式选择某个 MCP Tool。

本项目当前以内置 Function Calling/Tool Registry 为主：RAG、时间解析、工程师查询和预约 Service 与业务权限、事务、幂等、低延迟要求耦合较强，适合由应用直接维护严格 Schema，并在同一 Harness 中做鉴权和观测。Supervisor 调度咨询、预约等 Agent 节点使用 LangGraph 条件边或 `Command`，也不属于 MCP Tool Calling。

MCP 更适合边界清晰、需要被多个 AI 应用复用或由其他团队独立演进的连接器，例如 CRM、工单平台、企业文档资源。MCP Host 负责连接授权、能力白名单和用户同意；MCP Server 暴露最小工具集合。接入后仍必须在本地网关重新注入可信身份、检查租户和资源归属，并为写操作保留确认、幂等和审计。

不全上 MCP 的原因是：

- 内部函数已经能以更短路径满足单应用调用，协议化会增加网络、连接、序列化和故障面。
- 预约事务、排班冲突和幂等需要靠业务数据库约束，协议不能替代这些一致性规则。
- 动态发现大量工具会扩大模型选错和权限暴露面，仍需工具白名单与按节点裁剪。
- MCP Server 的版本、认证、租户隔离、超时、重试和可观测都需要额外治理。

因此边界原则是：跨应用复用和外部能力优先考虑 MCP；应用内部、强事务、低延迟的核心能力使用 Function Calling + Service；两者都只是能力接入方式，安全执行权始终在确定性业务层。

---

## 可核验参考

- [MCP：Architecture](https://modelcontextprotocol.io/specification/2025-06-18/architecture)
- [MCP Server：Prompts、Resources 与 Tools](https://modelcontextprotocol.io/specification/2025-06-18/server/index)
- [OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling)
